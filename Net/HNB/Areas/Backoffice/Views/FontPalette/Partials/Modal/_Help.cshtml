@{
    Layout = null;
}

<div id="fontPaletteHelpModal" class="modal fade" tabindex="-1" aria-labelledby="fontPaletteHelpModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-xl modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="fontPaletteHelpModalLabel">字體操作說明</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="關閉"></button>
            </div>
            <div class="modal-body">
                <div class="row g-4">
                    <div class="col-md-6">
                        <div class="vstack gap-3">
                            <section>
                                <h6 class="fw-bold mb-2 pb-2 border-bottom">功能說明</h6>
                                <p class="small mb-0">
                                    字體頁面提供系統中所有可用的字體變數，您可以瀏覽、搜尋並複製字體的 CSS 變數名稱。
                                    所有字體都定義在 CSS 變數中，方便統一管理和使用。
                                </p>
                            </section>

                            <section>
                                <h6 class="fw-bold mb-2 pb-2 border-bottom">操作說明</h6>
                                <table class="table table-sm table-bordered small mb-0">
                                    <thead class="table-light">
                                        <tr>
                                            <th style="width: 120px;">操作</th>
                                            <th>說明</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <tr>
                                            <td><strong>瀏覽字體</strong></td>
                                            <td>頁面會自動載入所有以 <code>--font-</code> 開頭的 CSS 變數，並以表格形式顯示。</td>
                                        </tr>
                                        <tr>
                                            <td><strong>搜尋字體</strong></td>
                                            <td>在搜尋框中輸入關鍵字，可搜尋字體名稱、CSS 變數名稱或字體值。</td>
                                        </tr>
                                        <tr>
                                            <td><strong>複製變數</strong></td>
                                            <td>點擊字體格子即可複製該字體的 CSS 變數名稱（例如：<code>var(--font-primary)</code>）。</td>
                                        </tr>
                                        <tr>
                                            <td><strong>預覽字體</strong></td>
                                            <td>每個字體格子會使用該字體本身來顯示字體名稱，方便預覽字體效果。</td>
                                        </tr>
                                    </tbody>
                                </table>
                            </section>

                            <section>
                                <h6 class="fw-bold mb-2 pb-2 border-bottom">字體分類</h6>
                                <ul class="small mb-0">
                                    <li><strong>Primary：</strong>主要字體，通常用於正文內容。</li>
                                    <li><strong>Secondary：</strong>次要字體，通常用於標題或強調文字。</li>
                                    <li><strong>Monospace：</strong>等寬字體，通常用於程式碼或表格。</li>
                                    <li><strong>Brand：</strong>品牌字體，通常用於品牌標識或特殊設計。</li>
                                    <li><strong>Custom：</strong>自訂字體，專案特定的字體設定。</li>
                                </ul>
                            </section>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="vstack gap-3">
                            <section>
                                <h6 class="fw-bold mb-2 pb-2 border-bottom">使用方式</h6>
                                <div class="small">
                                    <p class="mb-2"><strong>在 CSS 中使用：</strong></p>
                                    <pre class="bg-light p-2 rounded mb-3"><code>font-family: var(--font-primary);</code></pre>

                                    <p class="mb-2"><strong>在 HTML 中使用：</strong></p>
                                    <pre class="bg-light p-2 rounded mb-3"><code>&lt;div style="font-family: var(--font-primary);"&gt;
  文字內容
&lt;/div&gt;</code></pre>

                                    <p class="mb-2"><strong>在 JavaScript 中使用：</strong></p>
                                    <pre class="bg-light p-2 rounded mb-0"><code>element.style.fontFamily = 
  getComputedStyle(document.documentElement)
    .getPropertyValue('--font-primary');</code></pre>
                                </div>
                            </section>

                            <section>
                                <h6 class="fw-bold mb-2 pb-2 border-bottom">常見問題</h6>
                                <div class="small">
                                    <p class="mb-2"><strong>Q1：為什麼有些字體顯示不出來？</strong></p>
                                    <p class="ms-3 mb-3">A：部分字體（如 Google Fonts）需要先載入字體檔案，如果未載入會自動回退到系統字體。</p>

                                    <p class="mb-2"><strong>Q2：如何新增字體變數？</strong></p>
                                    <p class="ms-3 mb-3">A：在 <code>color-palette.css</code> 的 <code>:root</code> 區塊中新增 <code>--font-*</code> 變數，頁面會自動讀取並顯示。</p>

                                    <p class="mb-2"><strong>Q3：複製的變數如何使用？</strong></p>
                                    <p class="ms-3 mb-0">A：複製的變數名稱（如 <code>var(--font-primary)</code>）可以直接在 CSS 或 HTML 的 style 屬性中使用。</p>
                                </div>
                            </section>

                            <section class="alert alert-info small mb-0">
                                <h6 class="fw-bold mb-2">使用提示</h6>
                                <ul class="mb-0 ps-3">
                                    <li>建議使用 CSS 變數來管理字體，方便統一修改和維護。</li>
                                    <li>字體變數名稱建議使用有意義的命名，如 <code>--font-primary</code>、<code>--font-heading</code> 等。</li>
                                    <li>點擊字體格子即可快速複製 CSS 變數，無需手動輸入。</li>
                                    <li>搜尋功能支援部分匹配，輸入部分關鍵字即可找到相關字體。</li>
                                </ul>
                            </section>
                        </div>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary btn-sm" data-bs-dismiss="modal">我知道了</button>
            </div>
        </div>
    </div>
</div>


@{
    Layout = null;
}

<style>
    /* 只處理顏色相關的樣式，不處理布局 */
    
    /* 只保留必要的狀態提示顏色，不處理美觀裝飾 */
    
    /* 狀態提示 - 僅用於功能性提示 */
    .nav-status.active {
        background: #22c55e;
    }
    
    .nav-status.inactive {
        background: #9ca3af;
    }
    
    .status-display.active {
        color: #15803d;
    }
    
    .status-display.inactive {
        color: #6b7280;
    }
    
    .status-dot-large {
        background: currentColor;
    }
</style>


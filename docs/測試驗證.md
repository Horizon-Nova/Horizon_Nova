## 測試驗證記錄

### 使用方式
- **記錄格式**：`[時間] 測試項目 > 測試方法 > 步驟1 > 步驟2 > ... > 結果 > [失敗原因/修復方法]`
- **避免重複失敗**：測試前必須查看之前的測試記錄，避免重複使用已經失敗的測試方式
- **時間格式**：YYYY-MM-DD HH:MM 或 YYYY-MM-DD
### 範例：
[2024-01-01 10:00] 測試環境 > cmd > python --version > 成功
[2024-01-01 10:05] 測試環境 > cmd > pip install torch > 失敗 > 權限不足
[2024-01-01 10:10] 測試環境 > shell > pip install torch > 成功

### 測試記錄（時間軸格式）

[2026-02-14] `HNB_WEB/Views/Test/Index.cshtml` DarkVeil 測試頁載入驗證 > Windows PowerShell > `curl.exe -I -L https://unpkg.com/react@18/umd/react.development.js` > `curl.exe -I -L https://unpkg.com/react-dom@18/umd/react-dom.development.js` > `curl.exe -I -L https://esm.sh/ogl@0.0.78?bundle` > [成功] > CDN 回應正常（React/ReactDOM 302→200，`esm.sh` 200）；僅驗證資源存在
[2026-02-14] `HNB_WEB/Views/Test/Index.cshtml` 標語與 Hero 版面調整 > Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-14] `HNB_WEB/Views/Test/Index.cshtml` 背景切換（DarkVeil / 光暈 / 影片 / Logo） > Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-14] `HNB_WEB/Views/Test/Test*.cshtml` 五個背景展示頁（DarkVeil / Aurora / Grainient / Particles / Waves） > Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-14] `HNB_WEB/Views/NovaHome/Index.cshtml` 首頁改版（標題 / 頂置選單 / Hero 背景 / 服務互動區塊） > Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-14] `HNB_WEB` 畫面結構對齊（1 畫面 1 Controller + Index.cshtml + Partials） > Windows PowerShell > `dotnet build HNB/HNB.sln` > [失敗] > `dotnet watch` 佔用 `HNB.dll / HNB.exe` 造成檔案鎖定（MSB3021/MSB3027）
[2026-02-14] `HNB_WEB` 畫面結構對齊（1 畫面 1 Controller + Index.cshtml + Partials） > dotnet watch > 自動重建與重啟 > [成功] > 未出現 Razor 解析錯誤
[2026-02-14] `HNB_WEB/Views/NovaHome/Partials/_Showcase.cshtml` 專業服務項目配色與重複資訊調整 > dotnet watch > 自動重建與重啟 > [成功] > 服務區塊改深灰底；右側清單移除重複描述；左側 Tags 改由 `data-service-tags` 驅動
[2026-02-14] `HNB_WEB/Views/NovaHome/Index.cshtml` 全頁背景色彩統一（黑為主、灰為背景） > dotnet watch > 自動重建與重啟 > [成功] > About/Contact/Footer 由亮色系改為深灰系，避免白底跳色並提升專注度
[2026-02-14] `HNB_WEB/Views/Shared/_Layout.cshtml` `body` 背景色統一（黑為主、灰為背景） > dotnet watch > 自動重建與重啟 > [成功] > `body` 改為 `bg-slate-900`；`NovaHome` 外層改透明以顯示 body 底色
[2026-02-14] `HNB_WEB/Views/NovaHome` 移除不必要的 `bg-white`（避免白底跳色） > dotnet watch > 自動重建與重啟 > [成功] > CTA/導覽列按鈕由 `bg-white` 調整為 `bg-slate-100`（保留 `bg-white/5` 透明層作為深色 UI 細節）
[2026-02-14] `HNB_WEB/Views/NovaHome/Partials/_Showcase.cshtml` 服務展示移除重複內容（以動畫卡片取代右側面板） > dotnet watch > 自動重建與重啟 > [成功] > 左側保留清單切換；右側改為單一動畫內容區（UseCases/Deliverables/Tags/CTA 由同一份資料驅動）
[2026-02-14] `HNB_WEB/Views/NovaHome/Partials/_Showcase.cshtml` 專業服務項目左右留白與版面對齊（容器規格統一） > Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功] > `#services` 改為 `max-w-7xl + px-4/sm:px-6/lg:px-8`，與頂部導覽容器對齊，避免視覺「貼邊/不對位」
[2026-02-14] `HNB_WEB/Views/NovaHome/Partials/_Showcase.cshtml` 專業服務項目視覺層級強化（背景質感/卡片層/清單補充描述/Active 光暈） > Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功] > 加入漸層光暈與點陣背景、增加內層展示卡片、左側項目加入簡述、Active 狀態加入 Accent 光暈以降低單調感
[2026-02-14] `HNB_WEB/Views/NovaHome/Partials/_Showcase.cshtml` 專業服務項目介紹方式改為 Card Swap（卡片堆疊交換） > Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功] > 移除「左清單 + 右面板」介紹方式；改用卡片堆疊自動輪播＋點擊切換；並引入 GSAP CDN 於 `HNB_WEB/Views/NovaHome/Partials/_Scripts.cshtml`
[2026-02-14] `HNB_WEB/Views/NovaHome/Partials/_Showcase.cshtml` Card Swap 微調（卡片不透明/移除右側外框容器/標題置左避免被卡片遮擋） > Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功] > 卡片底色改 `bg-slate-950`；右側移除額外 `rounded-3xl + border + bg + blur` 容器；區塊標題改放左側欄位並提高 z-index
[2026-02-14] `HNB_WEB/Views/NovaHome/Index.cshtml` 關於我們區塊樣式翻新（降低單調、改為多卡片層級） > Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功] > 左側改為 4 張特色卡；右側改為雙卡（為什麼選擇我們／合作流程），整體維持深色不透明卡片與一致容器
[2026-02-14] `HNB_WEB/Views/NovaHome/Index.cshtml` 關於我們文案改為起源/理念（避免重複服務介紹，句首不再用「我們」） > Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-14] `HNB_WEB/Views/NovaHome/Index.cshtml` 關於我們起源文案語氣修正（改為正向品牌敘事，避免負面觀感） > Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-14] `HNB_WEB/Views/NovaHome/Index.cshtml` 關於我們文案對齊品牌定位（專業沉穩/社群協作起源/主打效率） > Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-14] `HNB_WEB/Views/NovaHome/Index.cshtml` 關於我們強化合作配合/需求釐清（主打好合作與節奏透明） > Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-14] `HNB_WEB/Views/NovaHome/Partials/_Showcase.cshtml` 專業服務項目內文語氣調整（移除「我們是...」開頭） > Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-14] `dbo.project` 資料表乾淨化（改名/刪除/合併/PK/Comment）+ 重新產生模型 + 前台同步 > Windows PowerShell > `psql.exe`（ALTER/UPDATE/COMMENT） > `dotnet run --project BuildModels/BuildModels.csproj` > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-17] `HNB_WEB/image` Icon PNG 轉 SVG（避免「隨便建立 SVG」） > Windows PowerShell > `python -c "...(inline)..."` > [失敗] > PowerShell 解析引號/大括號造成 ParserError（MissingArgument）
[2026-02-17] `HNB_WEB/image` Icon PNG 轉 SVG（SVG 內嵌原 PNG，確保圖樣一致） > Windows PowerShell > `python "tools/convert_png_to_svg.py"` > [成功] > 產出 `HNB/wwwroot/HNB_WEB/image/horizon-nova.svg`（由 `Horizon Nova Icon.png` 轉換）；另同步產出 `horizon-nova-logo.svg`（由 `Horizon Nova Logo.png` 轉換）
[2026-02-17] `HNB_WEB/image` 移除不需要的 `horizon-nova-logo.svg` > 檔案調整 > 刪除 `HNB/wwwroot/HNB_WEB/image/horizon-nova-logo.svg` 並更新 `tools/convert_png_to_svg.py` 停止產生 > [成功]
[2026-02-18] `HNB_WEB/Product` 產品頁原型（做法 B：/product + /product/{slug}）> Windows PowerShell > 新增 `ProductController` + `Views/Product/Index.cshtml` + `Views/Product/Detail.cshtml` > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-18] `HNB_WEB/Views/Portfolio/Partials/_Projects.cshtml` 主體成果改為產品型目錄（先用假資料做版面）> 檔案調整 > 改為「主打產品 + 產品目錄」並導向 `/product/{slug}` > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-18] `HNB_WEB/Views/NovaHome/Partials/_Showcase.cshtml` 專業服務項目卡片改版（移除 LED/光暈、改黑底白框 + 模擬網址列顯示 web/app/system/architecture 等）> Windows PowerShell > 檔案調整 > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-18] `HNB_WEB/Views/NovaHome/Partials/_Showcase.cshtml` 專業服務項目卡片調整（參考 Vercel Multi-tenant Platform：中上方關鍵字膠囊，不使用網址列）> Windows PowerShell > 檔案調整 > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-18] `HNB_WEB/Views/NovaHome/Partials/_Showcase.cshtml` 專業服務項目卡片內容清空（僅保留黑底白框 + 中上方 app/web/system/architecture 膠囊）> Windows PowerShell > 檔案調整 > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-18] `HNB_WEB/Views/NovaHome/Partials/_Showcase.cshtml` 專業服務項目 CardSwap 版型對齊範例（右下定位、圓角白框、每張卡單一標題、切換無光暈）> Windows PowerShell > 檔案調整 > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-18] `HNB_WEB/Views/NovaHome/Partials/_Scripts.cshtml` Card Swap 腳本外移（減少 Razor 內嵌）+ `HNB_WEB/Views/Shared/_Layout.cshtml` SEO 精簡（移除 keywords、簡化 robots、精簡 JSON-LD）> Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-18] `HNB_WEB/Views/NovaHome/Partials/_Showcase.cshtml` 移除 Card Swap 互動（改為純 Tailwind 服務卡片）+ `_Scripts.cshtml` 移除 GSAP/NovaHomeCardSwap 載入 + 刪除 `NovaHomeCardSwap.js` > Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-18] `HNB_WEB/Views/Shared/_Layout.cshtml` SEO 進一步簡化（僅保留 title + description，移除 robots/canonical/Open Graph/Twitter/JSON-LD）> Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-18] `HNB_WEB/NovaHome/Card_Swap.js` + `Card_Swap.css` 建立（以 id="CardSwap" + .hn-card 堆疊、CSS Transition 方式實作簡化版 Card Swap 動畫）+ `_Styles.cshtml` 引用 CSS + `_Scripts.cshtml` 清掉舊 GSAP 相關引用 > Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功]
[2026-02-18] `HNB_WEB/Views/NovaHome/Partials/_Showcase.cshtml` Card Swap 服務卡（標題改全繁中、圖片為主文字為輔、文案改為可交付描述）> Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功] > 0 錯誤；警告為既有專案警告（CS/EF）
[2026-02-18] `HNB_WEB/Views/NovaHome/Partials/_Showcase.cshtml` Card Swap 文案加強（摘要 + 交付/情境/整合重點清單；文字區固定高度可捲動，維持圖片為主）> Windows PowerShell > `dotnet build HNB/HNB.sln -c Release` > [成功] > 0 錯誤；警告為既有專案警告（CS）
## 測試驗證記錄

### 使用方式
- **記錄格式**：`[時間] 測試項目 > 測試方法 > 步驟1 > 步驟2 > ... > 結果 > [失敗原因/修復方法]`
- **避免重複失敗**：測試前必須查看之前的測試記錄，避免重複使用已經失敗的測試方式
- **時間格式**：YYYY-MM-DD HH:MM 或 YYYY-MM-DD
### 範例：
[2024-01-01 10:00] 測試環境 > cmd > python --version > 成功
[2024-01-01 10:05] 測試環境 > cmd > pip install torch > 失敗 > 權限不足
[2024-01-01 10:10] 測試環境 > shell > pip install torch > 成功

### 測試記錄（時間軸格式）
[2026-04-20] 修正建置錯誤（HNB.sln） > shell（PowerShell） > dotnet build HNB.sln（失敗） > 錯誤確認為 HNB.exe / HNB.dll 檔案鎖定（PID 33356） > taskkill /PID 33356 /F > dotnet build HNB.sln（成功） > 成功 > 修復方式：終止鎖定建置輸出檔的執行中程序後重新建置
[2026-04-20] 修正 WW 系統錯誤（DI 注入） > shell（PowerShell） > 檢查 DashboardController 與 ServiceExtensions 對應關係 > 發現注入 IWeatherService 但僅註冊 WeatherService > 修正為 AddHttpClient<IWeatherService, WeatherService>() > dotnet build HNB.sln（成功） > 成功 > 修復方式：補齊介面與實作的 DI 註冊對應
[2026-04-20] 修正 WW Dashboard THIS WEEK 只顯示 Mon > shell（PowerShell） > 檢查 Views/Dashboard/Index.cshtml > 發現 THIS WEEK 區塊硬編碼 Mon 且未使用 Model.WeatherDays > 改為 foreach 迴圈渲染 Model.WeatherDays（預期今日起 6 天） > dotnet build HNB.sln（成功） > 成功 > 修復方式：移除硬編碼並改為以天氣陣列驅動畫面
[2026-04-20] 重構 WW Dashboard 結構與 THIS WEEK 功能 > shell（PowerShell） > 移除 ViewModel 命名並改為 DashboardModel/WeatherDayModel > DashboardController 改用新 Model 並輸出今日起 6 天資料 > WeatherUtilities 新增 QueryWeatherEmoji 讓 THIS WEEK 圖示符合設計稿 > Dashboard/Index.cshtml 改為 wday 狀態樣式與點選切換（today/sel） > dotnet build HNB.sln（成功） > 成功 > 修復方式：完成 Model 命名、DI 介面注入、圖示映射與週天氣互動整合
[2026-04-20] 完成 WW Dashboard 設計稿互動狀態整合 > shell（PowerShell） > 擴充 Weather 預報資料（daily temperature_2m_max） > DashboardModel/WeatherDayModel 增加前端狀態欄位 > DashboardController 輸出 today/future 顯示資料與 Topbar 資訊 > Dashboard/Index.cshtml 重構為設計稿左右面板與六種右側狀態（empty/gen/items/result/record/future）並接入點選週曆與生成流程腳本 > 更新 _Layout.cshtml 的 topbar title id 供狀態切換 > dotnet build HNB.sln（成功） > 成功 > 修復方式：以模型驅動 UI + 前端狀態機實作設計稿互動
[2026-04-20] 修正 Dashboard 結果區缺少 tweak/shop 與按鈕樣式 > shell（PowerShell） > 補齊 Polished Rain-ready 區塊下方 Redo/Save icon、tweak chip 區、shop 卡片列 > 補齊 rec-badge/rec-weather 與 typewriter cursor 呈現 > showResult/backToEmpty 同步控制 r-tweak 與 shop-wrap 淡入淡出 > dotnet build HNB.sln（成功） > 成功 > 修復方式：補全設計稿結果區結構與互動狀態
[2026-04-21] 新增 WW 衣櫃頁面（Wardrobe）工作台風格 > shell（PowerShell） > 新增 WardrobeController / WardrobeIndexModel / WardrobeService / WardrobeUtilities > 新增 Wardrobe/Index.cshtml（向量查詢、分類、匯入提取、服裝清單）> 新增 wwwroot/ww/css/Wardrobe.css 與 wwwroot/ww/js/Wardrobe.js（查詢篩選、匯入佇列、提取模擬、待確認入庫）> 補齊 DI（ServiceExtensions）> dotnet build HNB.sln（成功） > 成功 > 驗證方式：以建置成功確認編譯與 DI 註冊無誤
[2026-04-21] 調整 WW 導覽與新增「紀錄」頁面（取代 Cal）> shell（PowerShell） > WW Shared/_Layout.cshtml 將 Cal 改為 紀錄（clock icon）並指向 HistoryController > Wardrobe 分類圖示改為色塊點（避免資料夾圖示）> 新增 HistoryController / HistoryIndexModel / HistoryService / HistoryUtilities / Views/History/Index.cshtml / wwwroot/ww/css/History.css / wwwroot/ww/js/History.js（搜尋、類型篩選、清單渲染）> 補齊 DI（ServiceExtensions）> dotnet build HNB.sln（成功） > 成功 > 失敗處理：建置遇到 HNB.exe 檔案鎖定（PID 38540）先 Stop-Process 後重建置成功
[2026-04-21] 調整 Wardrobe 分類 icon（改用 Lucide 圖示）並擴充衣櫃假資料 > shell（PowerShell） > Wardrobe/Index.cshtml 分類使用 shirt/columns-2/cloud/footprints/sparkles icon > WardrobeUtilities 擴充假資料數量與分類分佈 > dotnet build HNB.csproj -c Debug -p:UseAppHost=false -o ..\\artifacts\\_tmp_build_ww（成功） > 成功 > 說明：由於本機可能有執行中的 HNB.exe/HNB.dll 鎖定輸出，改以輸出到獨立資料夾驗證編譯
[2026-04-21] 擴充 History 假資料（用於驗證列表效果）> shell（PowerShell） > HistoryUtilities 生成近 10 天混合紀錄（匯入/提取/AI 生成/保存）並保留今日近況 > dotnet build HNB.csproj -c Debug -p:UseAppHost=false -o ..\\artifacts\\_tmp_build_history_fake（成功） > 成功 > 驗證方式：以建置成功確認編譯無誤並可在頁面觀察捲動與篩選效果
[2026-04-21] 修正 History 頁面空白問題（增加無 JS 回退與 JSON 命名相容）> shell（PowerShell） > History.js 將 records 正規化（支援 PascalCase/camelCase）避免欄位讀取為空 > History/Index.cshtml 在 #ww-history-list 先做伺服端預渲染並依資料量顯示/隱藏空狀態 > Wardrobe.js 同步補齊 PascalCase/camelCase 正規化以避免資料欄位空白 > dotnet build-server shutdown > dotnet build HNB.csproj -c Debug -p:UseAppHost=false -o ..\\artifacts\\_tmp_build_history_fix（成功） > 成功 > 失敗處理：遇到 HNB.sourcelink.json 檔案鎖定，透過 build-server shutdown 解除
[2026-04-21] 調整 History 右側呈現為「分日卡片區塊 + 內部網格」以統一 WW 工作台風格 > shell（PowerShell） > History/Index.cshtml 改為分日 section（日期標題 + 筆數 + 卡片網格）> History.css 新增 ww-day 與 ww-history-grid 並讓卡片背景與 WW 配色一致 > History.js 改為分日分組渲染並加上類型 icon，避免單純 list 造成風格落差 > Stop-Process（HNB）+ dotnet build-server shutdown > dotnet build HNB.csproj -c Debug -p:UseAppHost=false -o ..\\artifacts\\_tmp_build_history_style（成功） > 成功
[2026-04-21] 優化 History「清單過長」問題（先摘要、再下鑽、限制初始筆數）> shell（PowerShell） > History/Index.cshtml 新增摘要卡（今天/近7天）與載入更多容器 > History.css 新增摘要卡樣式、載入更多按鈕、日區塊收合樣式 > History.js 新增 limit（預設20）、預設僅展開前2天、其餘可點擊展開/收合、載入更多再追加20，並同步更新摘要 > 建置失敗處理：遇到 obj\\Debug\\net8.0\\HNB.dll 被 dotnet.exe（PID 6868）鎖定，Stop-Process 後重新建置成功 > dotnet build HNB.csproj -c Debug -p:UseAppHost=false -o ..\\artifacts\\_tmp_build_history_scrollfix（成功） > 成功
[2026-04-21] 重構 History 為「單日回看」工作台（左：日曆/搜尋/類型，中：當日操作卡，右：詳細資訊）> shell（PowerShell） > History/Index.cshtml 新增日期選擇與中間欄位（當日紀錄卡列表），右側改為詳情面板（對話/產出/提取前後示意）> History.css 新增 group card 與 detail 面板樣式並保留 WW 配色語言 > History.js 改為依單日聚合成少量操作卡（衣櫃更新、AI 生成等），點選後在右側呈現詳情 > 建置失敗處理：遇到 obj\\Debug\\net8.0\\HNB.dll 被 dotnet.exe（PID 13736）鎖定，Stop-Process 後重新建置成功 > dotnet build HNB.csproj -c Debug -p:UseAppHost=false -o ..\\artifacts\\_tmp_build_history_3col（成功） > 成功
[2026-04-21] 修正 WW Sidebar 點擊無反應（啟用 Razor TagHelpers）> shell（PowerShell） > 新增 Areas/WW/Views/_ViewImports.cshtml 加入 @addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers 讓 asp-area/asp-controller/asp-action 生效並產生 href > dotnet build-server shutdown > dotnet build HNB.csproj -c Debug -p:UseAppHost=false -o ..\\artifacts\\_tmp_build_viewimports（成功） > 成功

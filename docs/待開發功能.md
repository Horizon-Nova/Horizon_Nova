## 待開發功能（需求清單）

### 使用方式
- **新增需求**：請直接在下方「下一個需求」貼上需求文字（建議逐條列點）
- **避免跑版**：請包含「期望行為 / 觸發條件 / 不做的範圍」
### 範例
[已完成] 1. 終端機的錯誤訊息要用紅字顯示  
[已完成] 2. 添加 key 鎖在 Main.java  
[已完成] 3. 查詢結果要添加「未找到目標」  
[已完成] 4. 添加 ErrorLog 機制：在既有 HTTP 添加本地紀錄，存放在 `C:\InSpecTool\ErrorLog\[當前時間 格式0000-00-00]-ErrorLog.log`  
[須修正] 5. 修正 InSpecToolTestArea 工具「點擊視窗、點擊模式」：應右邊顯示所有檢測資訊，並非刻意限制顯示按鈕  

### 下一個需求（請貼在這裡）
- （尚未提供）

### 需求清單

### 測試記錄
[失敗] 2026-04-29 `dotnet build .\\HNB\\HNB.csproj`：因 `HNB.exe` 被執行中程序鎖定（鎖定者: `HNB (39664)`）導致複製輸出檔失敗（MSB3027/MSB3021）。
[成功] 2026-04-29 `dotnet build .\\HNB\\HNB.csproj -t:Compile`：0 errors（warnings 既有，與本次變更無直接關聯）。
[成功] 2026-04-29 Today Razor 驗證：修正 `.cshtml` 內 CSS 的 `@keyframes`（需使用 `@@keyframes`）後，`dotnet build .\\HNB\\HNB.csproj -t:Compile` 仍為 0 errors。
[成功] 2026-04-29 Closet Razor 驗證：`dotnet build .\\HNB\\HNB.csproj -t:Compile` 0 errors；Closet 已移除 `~/ww/css/Closet.css`、`~/ww/js/Closet.js` 引用，改由 `Views/Closet/Partials/_Styles.cshtml`、`_Scripts.cshtml` 內嵌。
[成功] 2026-04-29 Closet 卷軸調整：鎖定 `ww-feature-closet` 高度並讓右側 grid 內部捲動，避免右側內容撐高導致左側 Upload 區塊被拉長；`dotnet build .\\HNB\\HNB.csproj -t:Compile` 0 errors。
[成功] 2026-04-29 Closet 手機版佈局：小螢幕改為「匯入在上、衣櫃在下」，並僅透過高度/overflow 讓右側 grid 內部捲動；`dotnet build .\\HNB\\HNB.csproj -t:Compile` 0 errors。

[成功] 2026-04-29 ErrorLog：ErrorLogService.Save() 新增同步寫入 dbo.error_logs，並在 DI 註冊 ErrorLogRepository；以 dotnet build .\HNB\HNB.csproj 驗證 0 errors（有 warnings）。
[成功] 2026-04-29 WW Calendar(Outfit Log)：依 wtw_log_v4.html 重做 Views/Calendar，改用 Layout + Head section，移除引用 ~/ww/css/Calendar.css、~/ww/js/Calendar.js，改成 inline _Styles/_Scripts；以 dotnet build .\HNB\HNB.csproj 驗證 0 errors（有 warnings）。
[成功] 2026-04-29 WW Layout：Sidebar 依 RouteData(controller) 自動切換 active 樣式，並補上 Calendar 連結；以 dotnet build .\HNB\HNB.csproj 驗證 0 errors（有 warnings）。
[成功] 2026-04-29 WW Calendar(Outfit Log)：移除大量 CSS，改以 Tailwind class 重寫 Topbar/MainContent/卡片與日曆格；_Styles.cshtml 僅保留 @keyframes 與 scrollbar；以 dotnet build .\HNB\HNB.csproj 驗證 0 errors（有 warnings）。
[成功] 2026-04-29 Closet：補回 .main flex 版面（手機上下、桌機左右兩欄），修正 Upload 與 Grid 不應該全部堆疊的問題；以 dotnet build .\HNB\HNB.csproj 驗證 0 errors（有 warnings）。
[成功] 2026-04-30 WW Calendar(Outfit Log)：卡片結構調整（AI Suggestion / Your Photo / Items worn / weather pill icon），動態 render 後補 `lucide.createIcons()`；以 `dotnet build -p:OutputPath=bin\\buildcheck\\ -p:UseAppHost=false` 驗證 0 errors（有 warnings）。
[成功] 2026-04-30 WW Closet：右側衣櫃 grid 捲動區增加底部 padding（避免 footer 蓋住造成最後一排卡片被切掉）；以 `dotnet build -p:OutputPath=bin\\buildcheck\\ -p:UseAppHost=false` 驗證 0 errors（有 warnings）。
[成功] 2026-04-30 WW Calendar(Outfit Log)：Your Photo 右半部改為「上方照片區 + 下方淺色條」並調整關閉按鈕與 Share 按鈕位置貼近藍圖；以 `dotnet build -p:OutputPath=bin\\buildcheck\\ -p:UseAppHost=false` 驗證 0 errors（有 warnings）。
[成功] 2026-04-30 WW Auth：改為使用 `Areas/WW/Views/Shared/_Layout.cshtml`，移除 `~/ww/css/Auth.css`、`~/ww/js/Auth.js` 引用，改用 Tailwind + inline script（tab、顯示密碼、密碼強度、context pill）；以 `dotnet build -p:OutputPath=bin\\buildcheck\\ -p:UseAppHost=false` 驗證 0 errors（有 warnings）。
[成功] 2026-04-30 WW Auth：依需求修正為「不使用 Layout」，改為 Auth 獨立頁面自行引入 Tailwind CDN / Fonts / Lucide；以 `dotnet build -p:OutputPath=bin\\buildcheck\\ -p:UseAppHost=false` 驗證 0 errors（有 warnings）。
[成功] 2026-04-30 WW Layout：依 wtw_profile_drawer.html 增加 sidebar hover 展開效果、左下角 avatar，並新增 Profile Drawer（overlay + 右側抽屜），ESC/點遮罩/關閉鈕可關閉；以 `dotnet build -p:OutputPath=bin\\buildcheck\\ -p:UseAppHost=false` 驗證 0 errors（有 warnings）。
